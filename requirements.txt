# VistaraBI — Python Dependencies for Module 8 Forecasting Engine
# Used by: scripts/forecast_bridge.py (Prophet bridge for baseline forecasting)
#
# Installation:
#   pip install -r requirements.txt
#
# Note: If Prophet is NOT installed, the system gracefully falls back to
#       the TypeScript linear regression forecaster in prophet-bridge.ts.
#       The application is fully functional without Python installed.

# Meta Prophet — Time Series Forecasting Engine
# https://facebook.github.io/prophet/
prophet==1.1.5

# Data manipulation (required by Prophet)
pandas>=1.5.0,<2.2.0

# Scientific computing (required by pandas + Prophet internals)
numpy>=1.24.0,<2.0.0

# Holidays dataset (optional Prophet dependency, improves seasonality)
holidays>=0.20

# Optional: Plotly for local visualization during development
# plotly>=5.0.0
